# Slim Python base — no ESPHome toolchain is bundled anymore; all builds are
# delegated to the ESPHome Device Builder dashboard. This removes the pinned
# esphome version that used to reject configs needing newer features.
build_from:
  aarch64: python:3.12-slim
  amd64: python:3.12-slim
